import { extendTheme } from "@chakra-ui/react";

const CustomTheme = extendTheme({
  colors: {
    brand: {
      primary: "#3110a7",
      primaryDark: "#3110a7",
      accent: "#3110a7",
    },
  },
});
export default CustomTheme;
